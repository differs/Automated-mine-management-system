name: shared
description: "共享模块 - 离线调度 + 车牌识别"
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.0
  uuid: ^4.5.0
  google_mlkit_text_recognition: ^0.14.0
  image_picker: ^1.1.0
  http: ^1.2.0
  sqflite: ^2.4.0
  path: ^1.9.0
